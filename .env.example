# T0 requires all three. The service refuses to start a run without them
# rather than spending credits on a partial pipeline.
FIRECRAWL_API_KEY=fc-...
EXA_API_KEY=...
XAI_API_KEY=xai-...

# Optional overrides
# GROK_MODEL=grok-4.6
# XAI_BASE_URL=https://api.x.ai/v1
